# Empty means same origin: the app requests /api/... and the Vite dev proxy
# (see vite.config.js) forwards to the backend. Set an absolute origin only for
# a deployment where the API is not served alongside the frontend. Removing the
# line entirely is different from leaving it empty — unset turns on demo stubs.
VITE_API_URL=
